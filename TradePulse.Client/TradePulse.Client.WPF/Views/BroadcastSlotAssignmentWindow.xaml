<Window x:Class="TradePulse.Client.WPF.Views.BroadcastSlotAssignmentWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Assign Broadcast" Height="420" Width="520"
        WindowStartupLocation="CenterOwner"
        Background="#1b1b1b"
        FontFamily="Segoe UI">

    <Window.Resources>
        <SolidColorBrush x:Key="AssignWindowBackground" Color="#1b1b1b"/>
        <SolidColorBrush x:Key="AssignControlBackground" Color="#2a2a2a"/>
        <SolidColorBrush x:Key="AssignControlBorder" Color="#3a3a3a"/>
        <SolidColorBrush x:Key="AssignText" Color="#ffffff"/>
        <SolidColorBrush x:Key="AssignTextSecondary" Color="#cfcfcf"/>
        <SolidColorBrush x:Key="AssignSelection" Color="#0078d4"/>

        <Style TargetType="TextBox">
            <Setter Property="Background" Value="{StaticResource AssignControlBackground}"/>
            <Setter Property="Foreground" Value="{StaticResource AssignText}"/>
            <Setter Property="CaretBrush" Value="{StaticResource AssignText}"/>
            <Setter Property="BorderBrush" Value="{StaticResource AssignControlBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,4"/>
        </Style>

        <Style TargetType="ListBox">
            <Setter Property="Background" Value="{StaticResource AssignControlBackground}"/>
            <Setter Property="Foreground" Value="{StaticResource AssignText}"/>
            <Setter Property="BorderBrush" Value="{StaticResource AssignControlBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <Style TargetType="ListBoxItem">
            <Setter Property="Foreground" Value="{StaticResource AssignText}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="{StaticResource AssignSelection}"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#334155"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="Button">
            <Setter Property="Background" Value="{StaticResource AssignControlBackground}"/>
            <Setter Property="Foreground" Value="{StaticResource AssignText}"/>
            <Setter Property="BorderBrush" Value="{StaticResource AssignControlBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,6"/>
        </Style>
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Select a broadcast to assign to this button" Foreground="White" FontSize="14" FontWeight="600" Margin="0,0,0,10"/>

        <TextBox Grid.Row="1" x:Name="SearchTextBox" Height="32" Margin="0,0,0,10"/>

        <ListBox Grid.Row="2" x:Name="ItemsList" DisplayMemberPath="Name"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button x:Name="ClearButton" Content="Clear" MinWidth="90" Margin="0,0,8,0"/>
            <Button x:Name="CancelButton" Content="Cancel" MinWidth="90" Margin="0,0,8,0"/>
            <Button x:Name="AssignButton" Content="Assign" MinWidth="90"/>
        </StackPanel>
    </Grid>
</Window>
